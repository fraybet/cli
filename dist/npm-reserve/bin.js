#!/usr/bin/env node
// Placeholder bin for the reserved @fraybet/cli name. The full CLI ships with
// the v0.1.0 release (goreleaser + the postinstall downloader in dist/npm).
console.log(`Fray CLI — the full release is on its way.

Install now:
  curl -fsSL https://fray.bet/install.sh | sh    # script installer
  brew install fraybet/tap/fray                  # Homebrew

Docs:  https://fray.bet/docs
`);
